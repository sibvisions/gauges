# UI Gauges
A collection of SVG based gauges

## Available Gauges:
 - Ring
 - Arc
 - Meter
 - Speedometer

## Requirements
 - node & npm

## Build
 - run `npm install`
 - run `npm run build`
 or
 - run `npm run watch` during development